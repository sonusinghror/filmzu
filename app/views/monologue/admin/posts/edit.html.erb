<div class="col-sm-12 new-blog-post">
    <h3><%= t(".header") %> "<%= @post.posts_revisions.last.title %>"</h3>
    <%= monologue_admin_form_for ["admin", @post], :method => :put do |post| %>
      <%= render :partial => 'form', :locals => {:post => post} %>
    <% end %>
</div>